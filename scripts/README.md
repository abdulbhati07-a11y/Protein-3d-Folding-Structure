# Scripts

| Script | Description |
|--------|-------------|
| `run_backend.ps1` | Start Flask API on port 5000 |
| `run_frontend.ps1` | Start Vite dev server on port 5173 |

Run from project root or invoke directly:

```powershell
.\scripts\run_backend.ps1
.\scripts\run_frontend.ps1
```
